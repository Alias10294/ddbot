services:
  backend:
    build:
      context: ./backend
    container_name: security-agent-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/app:/backend/app
    restart: unless-stopped